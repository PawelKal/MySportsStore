
@model IEnumerable<string>

@{ 
    bool horizontal = ((bool)(ViewContext.RouteData.Values["horizontalLayout"] ?? false));
    string wrapperClasses = horizontal ? "btn-group btn-group-sm btn-group-justified" : null;

}
<div class="@wrapperClasses">
    @Html.ActionLink("Home", "List", "Product", null,
    new { @class = "btn btn-info btn-block btn-lg" })
@foreach (var link in Model)
{
    @Html.RouteLink(link, new
{
    controller = "Product",
    action = "List",
    category = link,
    page = 1
}, new
{
    @class = (link == ViewBag.SelectedCategory ? "btn btn-block btn-default btn-lg" : "btn btn-block btn-info btn-lg")
})
}
</div>
